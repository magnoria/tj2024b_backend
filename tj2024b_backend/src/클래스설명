
[객체]
	- 의사나 행위가 미치는 대상
	
	- 객체
[객체지향 프로그래밍]
	- 어떤 대상(객체)을 가지고 프로그래밍한다.
[클래스]
	- 객체의 속성과 기능을 코드로 구현한것
		- 속성 : 대상의 본질적인 성질/특징/특성        멤버변수
		- 기능 : 대상의 작동/행위/이벤트			메소드(멤버함수)
	- 클래스 형태
		class 클래스명 { 멤버변수; 메소드;}
		
			* 클래스 이름 짓는 규칙 : 대문자로 시작한다.
		
		-* 객체를 만들기 위한 객체의 일종의 틀(설계도)
		
	- 클래스 사용처 : 1. 객체 설계도 클래스 2. main함수 갖는 실행 클래스
	
	
			학생 클래스 코드 표현
		public class Student {
			int studentID;
			Strign studentName;
			int grade;
			String address;
			
		}
		
			자동차 클래스 코드 표현
		public class car{
			String 제조사;
			Sttring 차량번호;
			int 현재속도;
		}
		


[메소드] 멤버함수 또는 메소드라 불림
	1. 정의 : 하나의 기능을 수행하는 일련의 코드 , 여러 코드(명령어;)들을 묶음으로 저장하는 역할
	2. 함수 정의/만들기
			자바스크립트의 fuction 기능 과 같다
		타입타입 함수명(타입 매개변수1, 타입 매개변수2){
			실행코드;
			return 반환값 | 반환값이 없으면 생략
		}
		1. 함수 이름: 변수명과 동일하게 임의, 카멜표기법 권장
		2. 매개변수 : 함수의 입력으로 받는 변수 * 
				1.매개변수는 없을 수도 있다.
				2. 함수 호출/ 사용시 인수값을 대입받는 변수
				3. 인수값의 타입과 매개변수 타입은 일치 해야한다.
				
				
				
		3. 반환값 : 함수를 수행한후 결과로 되돌려 주는 값 * 반환값도 없을수도 있다 * 반환타입이 없을때 void 타입으로
			- return : 1. 반환값 되돌려 주는 역할 2. 함수 종료
					   2. 반환값도 없을수도 있다 * 반환타입이 없을때 void 타입으로
					   3. 반환값의 타입과 반환타입은 일치해야한다.
	
* 참고 : 자바에서 오류가 발생했을때 대처방법
1. 일단 실행 -> 콘솔에 오류메시지 확인후 -> 구글링

*참고 : 자바의 이름짓기 규약
	- 패키지/폴더명은 모두 소문자로 한다.
	- 클래스명은 첫글자를 대문자로 시작한다.
	- 변수/함수명은 첫글자를 소문자로 시작하고 중간에 새 단어의 첫글자를 대문자로 한다.
		- 카멜 표기법 사용한다.
	- 객체명/배열명 : 존재하지않는다.
		타입 변수명 = 객체   * 관례적으로 객체를 참조하는 변수는 객체변수라고 한다.
		타입[] 변수명 = 배열	* 관례적으로 배열을 참조하는 변수는 배열변수라고 한다.
	
	
	